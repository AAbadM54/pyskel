language: python
python:
  - 3.5
# command to install dependencies
install:
  - pip install -r requirements.txt
env:
  - Environment: qa
# command to run tests
script: python3 -m unittest discover -s test -p "test_*.py" -v
